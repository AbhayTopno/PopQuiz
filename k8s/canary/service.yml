apiVersion: v1
kind: Service
metadata:
  name: popquiz-redis
  namespace: popquiz
spec:
  clusterIP: None
  selector:
    app: popquiz-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379

---
apiVersion: v1
kind: Service
metadata:
  name: popquiz-qdrant
  namespace: popquiz
spec:
  clusterIP: None
  selector:
    app: popquiz-qdrant
  ports:
    - name: http
      protocol: TCP
      port: 6333
      targetPort: 6333
    - name: grpc
      protocol: TCP
      port: 6334
      targetPort: 6334

---
apiVersion: v1
kind: Service
metadata:
  name: popquiz-langchain-stable
  namespace: popquiz
spec:
  selector:
    app: popquiz-langchain
    version: stable
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: popquiz-langchain-canary
  namespace: popquiz
spec:
  selector:
    app: popquiz-langchain
    version: canary
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: popquiz-backend-stable
  namespace: popquiz
spec:
  selector:
    app: popquiz-backend
    version: stable
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: popquiz-backend-canary
  namespace: popquiz
spec:
  selector:
    app: popquiz-backend
    version: canary
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: popquiz-frontend-stable
  namespace: popquiz
spec:
  selector:
    app: popquiz-frontend
    version: stable
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: popquiz-frontend-canary
  namespace: popquiz
spec:
  selector:
    app: popquiz-frontend
    version: canary
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
