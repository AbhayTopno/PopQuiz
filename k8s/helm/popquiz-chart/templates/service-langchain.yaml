apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.langchain.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.langchain.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.langchain.port }}
  type: ClusterIP
