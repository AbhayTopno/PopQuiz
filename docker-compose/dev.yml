---
services:
  backend:
    build:
      context: ../backend
      dockerfile: docker/Dockerfile.dev
    container_name: backend_dev
    ports:
      - '5000:5000'
    env_file:
      - .env.backend
    environment:
      - LANGCHAIN_SERVICE_URL=http://langchain:8000
    volumes:
      - ../backend:/usr/src/app:cached # Mount backend source code
      - /usr/src/app/node_modules # Prevent overwriting node_modules
      - /usr/src/app/dist # (optional) keep build artifacts inside container
    command: pnpm run dev
    networks:
      - myapp-network
    depends_on:
      - redis
      - langchain

  frontend:
    build:
      context: ../frontend
      dockerfile: docker/Dockerfile.dev
    container_name: frontend_dev
    ports:
      - '3000:3000'
    env_file:
      - .env.frontend
    volumes:
      - ../frontend:/usr/src/app:cached # Mount frontend code for live reload
      - /usr/src/app/node_modules # Prevent overwriting node_modules
      - /usr/src/app/.next # (optional) preserve Next.js cache
    depends_on:
      - backend
    command: pnpm run dev # Use Next.js dev mode
    networks:
      - myapp-network

  langchain:
    build:
      context: ../langchain
      dockerfile: docker/Dockerfile.dev
    container_name: langchain_dev
    ports:
      - '8000:8000'
    env_file:
      - .env.langchain
    volumes:
      - ../langchain:/usr/src/app:cached # Mount app code for hot reload
      - /usr/src/app/.venv # Prevent Windows host .venv from overwriting Linux container .venv
    networks:
      - myapp-network
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: redis_dev
    ports:
      - '6379:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - myapp-network

networks:
  myapp-network:
